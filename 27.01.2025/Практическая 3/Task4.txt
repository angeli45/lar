@startuml
start
:**ввод** x;
if(x>=1) then(да)
:y = x * (pow(x, 4) + 3 * x) + log(PI + x);
else(нет)
if (x<0) then (да)
:y=pow(x, 5) - 10 - pow(PI + x, 1.0 / 4);
else (нет)
:y = pow(pow(x, 3) + 3, 3) - sqrt(0.5 * PI + x);
endif
endif
:**вывод** y;
stop
@enduml